---
phase: 02-test-infrastructure-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gradle/libs.versions.toml
  - build.gradle.kts
  - src/test/kotlin/com/github/zzave/ynabsplitpayeeandmemo/e2e/WireMockTestBase.kt
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "WireMock server starts before each test"
    - "WireMock server stops after each test without manual cleanup"
    - "Tests can stub YNAB API v1 endpoints with custom responses"
  artifacts:
    - path: "gradle/libs.versions.toml"
      provides: "WireMock dependency version"
      contains: "wiremock"
    - path: "build.gradle.kts"
      provides: "WireMock test dependency"
      contains: "wiremock"
    - path: "src/test/kotlin/com/github/zzave/ynabsplitpayeeandmemo/e2e/WireMockTestBase.kt"
      provides: "Base test class with WireMock lifecycle management"
      min_lines: 40
      exports: ["WireMockTestBase"]
  key_links:
    - from: "src/test/kotlin/com/github/zzave/ynabsplitpayeeandmemo/e2e/WireMockTestBase.kt"
      to: "WireMock.startServer()"
      via: "beforeSpec hook"
      pattern: "beforeSpec.*WireMock"
    - from: "src/test/kotlin/com/github/zzave/ynabsplitpayeeandmemo/e2e/WireMockTestBase.kt"
      to: "WireMock.stopServer()"
      via: "afterSpec hook"
      pattern: "afterSpec.*WireMock"
---

<objective>
Establish WireMock mock server infrastructure for E2E tests that automatically starts/stops per test class without manual cleanup.

Purpose: Enable E2E tests to run against a local HTTP mock server that simulates the YNAB API v1 without hitting the real API.
Output: WireMock dependency configured, base test class with automatic server lifecycle management.
</objective>

<execution_context>
@/Users/julius.van.dis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julius.van.dis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cli-configuration/01-01-SUMMARY.md
@CLAUDE.md
@build.gradle.kts
@gradle/libs.versions.toml
@src/test/kotlin/com/github/zzave/ynabsplitpayeeandmemo/TransactionUpdaterTest.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add WireMock dependency to Gradle</name>
  <files>gradle/libs.versions.toml, build.gradle.kts</files>
  <behavior>
    - WireMock standalone version added to versions catalog
    - WireMock added as testImplementation dependency
    - Gradle sync succeeds without errors
  </behavior>
  <action>
Add WireMock 3.x standalone dependency to the project for HTTP mock server testing.

**gradle/libs.versions.toml:**
- Add `wiremock = "3.10.0"` to `[versions]` section
- Add `wiremock = { group = "org.wiremock", name = "wiremock-standalone", version.ref = "wiremock" }` to `[libraries]` section

**build.gradle.kts:**
- Add `testImplementation(libs.wiremock)` to `dependencies` block

Use WireMock standalone (not wiremock-jre8 or wiremock) for JVM 21 compatibility.
  </action>
  <verify>
    <automated>./gradlew dependencies --configuration testRuntimeClasspath | grep wiremock</automated>
  </verify>
  <done>WireMock 3.10.0 appears in test dependencies, Gradle sync completes successfully</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create WireMockTestBase with automatic lifecycle management</name>
  <files>src/test/kotlin/com/github/zzave/ynabsplitpayeeandmemo/e2e/WireMockTestBase.kt</files>
  <behavior>
    - Test: WireMock server starts in beforeSpec hook
    - Test: WireMock server stops in afterSpec hook
    - Test: Server URL is accessible via property
    - Test: Server responds to stubbed requests
  </behavior>
  <action>
Create a Kotest FunSpec base class that manages WireMock server lifecycle automatically using beforeSpec/afterSpec hooks.

**File:** `src/test/kotlin/com/github/zzave/ynabsplitpayeeandmemo/e2e/WireMockTestBase.kt`

**Implementation:**
- Create `WireMockTestBase` abstract class extending Kotest `FunSpec`
- Use `beforeSpec` hook to start WireMock server on dynamic port
- Use `afterSpec` hook to stop WireMock server
- Expose `wireMockServer` property (WireMockServer instance)
- Expose `apiBaseUrl` property returning `http://localhost:{port}/v1` for CLI --api-url flag
- Configure WireMock with minimal verbosity (not logging every request)

**Kotest hooks pattern:**
```kotlin
init {
    beforeSpec {
        wireMockServer = WireMockServer(WireMockConfiguration.options().dynamicPort())
        wireMockServer.start()
    }

    afterSpec {
        wireMockServer.stop()
    }
}
```

**Why base class:** Test classes extending this get automatic server lifecycle. No manual setup/teardown needed.
  </action>
  <verify>
    <automated>make test TEST="*WireMockTestBase*"</automated>
  </verify>
  <done>WireMockTestBase compiles, test demonstrates server starts/stops, exposes apiBaseUrl property</done>
</task>

</tasks>

<verification>
**Dependency verification:**
```bash
./gradlew dependencies --configuration testRuntimeClasspath | grep wiremock
```
Should show `org.wiremock:wiremock-standalone:3.10.0`

**Base class verification:**
```bash
make test TEST="*WireMockTestBase*"
```
Should pass with WireMock server lifecycle tests

**File existence:**
```bash
ls src/test/kotlin/com/github/zzave/ynabsplitpayeeandmemo/e2e/WireMockTestBase.kt
```
Should exist
</verification>

<success_criteria>
1. WireMock 3.10.0 standalone dependency added and resolves successfully
2. WireMockTestBase class provides automatic server start/stop via Kotest hooks
3. Base class exposes `apiBaseUrl` property for CLI --api-url injection
4. All tests pass including new WireMockTestBase verification tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-test-infrastructure-setup/02-01-SUMMARY.md`
</output>
