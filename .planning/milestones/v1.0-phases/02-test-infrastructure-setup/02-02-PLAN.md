---
phase: 02-test-infrastructure-setup
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/test/kotlin/com/github/zzave/ynabsplitpayeeandmemo/e2e/CliRunner.kt
  - src/test/kotlin/com/github/zzave/ynabsplitpayeeandmemo/e2e/CliResult.kt
  - src/test/kotlin/com/github/zzave/ynabsplitpayeeandmemo/e2e/SmokeTest.kt
autonomous: true
requirements: [TEST-02, TEST-03]

must_haves:
  truths:
    - "Tests invoke the built test JAR as a real subprocess"
    - "Tests can pass --api-url flag to point CLI at mock server"
    - "Tests can capture CLI exit code, stdout, and stderr"
  artifacts:
    - path: "src/test/kotlin/com/github/zzave/ynabsplitpayeeandmemo/e2e/CliRunner.kt"
      provides: "ProcessBuilder CLI invocation helper"
      min_lines: 40
      exports: ["runCli"]
    - path: "src/test/kotlin/com/github/zzave/ynabsplitpayeeandmemo/e2e/CliResult.kt"
      provides: "CLI result data class capturing output"
      exports: ["CliResult"]
    - path: "src/test/kotlin/com/github/zzave/ynabsplitpayeeandmemo/e2e/SmokeTest.kt"
      provides: "End-to-end smoke test scaffold"
      min_lines: 30
  key_links:
    - from: "src/test/kotlin/com/github/zzave/ynabsplitpayeeandmemo/e2e/SmokeTest.kt"
      to: "CliRunner.runCli()"
      via: "test invocation"
      pattern: "runCli"
    - from: "src/test/kotlin/com/github/zzave/ynabsplitpayeeandmemo/e2e/CliRunner.kt"
      to: "ProcessBuilder"
      via: "subprocess execution"
      pattern: "ProcessBuilder"
    - from: "src/test/kotlin/com/github/zzave/ynabsplitpayeeandmemo/e2e/SmokeTest.kt"
      to: "WireMockTestBase.apiBaseUrl"
      via: "--api-url flag"
      pattern: "apiBaseUrl"
---

<objective>
Create ProcessBuilder-based CLI invocation helper and smoke test scaffold that validates the E2E test infrastructure works end-to-end.

Purpose: Enable E2E tests to invoke the actual test JAR as a subprocess and capture its output for assertions.
Output: CliRunner helper, CliResult data class, and smoke test demonstrating WireMock + ProcessBuilder integration.
</objective>

<execution_context>
@/Users/julius.van.dis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julius.van.dis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cli-configuration/01-01-SUMMARY.md
@.planning/phases/02-test-infrastructure-setup/02-01-SUMMARY.md
@CLAUDE.md
@src/test/kotlin/com/github/zzave/ynabsplitpayeeandmemo/TransactionUpdaterTest.kt

<interfaces>
<!-- Key types from WireMockTestBase (Plan 02-01) -->
From src/test/kotlin/com/github/zzave/ynabsplitpayeeandmemo/e2e/WireMockTestBase.kt:
```kotlin
abstract class WireMockTestBase : FunSpec() {
    protected lateinit var wireMockServer: WireMockServer
    val apiBaseUrl: String
        get() = "http://localhost:${wireMockServer.port()}/v1"
}
```

<!-- CLI infrastructure from Phase 1 -->
From Phase 01-01-SUMMARY.md:
- Test JAR built with: `./gradlew shadowJar -PtestBuild=true`
- Test JAR location: `build/libs/ynab-split-payee-and-memo-*-all.jar`
- Test JAR accepts: `--api-url`, `--token`, `--dry-run` flags
- Production builds reject `--api-url` with error via echo()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CliResult data class and CliRunner helper</name>
  <files>src/test/kotlin/com/github/zzave/ynabsplitpayeeandmemo/e2e/CliResult.kt, src/test/kotlin/com/github/zzave/ynabsplitpayeeandmemo/e2e/CliRunner.kt</files>
  <behavior>
    - Test: CliResult captures exit code, stdout, stderr
    - Test: runCli() function builds correct ProcessBuilder command
    - Test: runCli() finds test JAR automatically via glob pattern
    - Test: runCli() waits for process completion and captures all output
  </behavior>
  <action>
Create ProcessBuilder-based CLI invocation helper that runs the test JAR as a subprocess.

**File 1:** `src/test/kotlin/com/github/zzave/ynabsplitpayeeandmemo/e2e/CliResult.kt`
```kotlin
data class CliResult(
    val exitCode: Int,
    val stdout: String,
    val stderr: String
)
```

**File 2:** `src/test/kotlin/com/github/zzave/ynabsplitpayeeandmemo/e2e/CliRunner.kt`

**Implementation:**
- Create `runCli(vararg args: String): CliResult` function
- Find test JAR path using `File("build/libs").listFiles()` and glob pattern matching `*-all.jar`
- Build command: `java -jar {jarPath} {args}`
- Use ProcessBuilder to execute command
- Capture stdout and stderr using `process.inputStream.bufferedReader().readText()`
- Wait for process completion with `process.waitFor()`
- Return CliResult with exit code and captured output

**Error handling:**
- If JAR not found, throw clear exception: "Test JAR not found. Run: ./gradlew shadowJar -PtestBuild=true"
- If multiple JARs found, use the newest by lastModified timestamp

**Why ProcessBuilder:** Validates the actual CLI behavior including argument parsing, BuildInfo checks, and YnabClient initialization.
  </action>
  <verify>
    <automated>make test TEST="*CliRunner*"</automated>
  </verify>
  <done>CliRunner.runCli() finds test JAR, executes subprocess, captures output in CliResult data class</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create smoke test demonstrating full E2E infrastructure</name>
  <files>src/test/kotlin/com/github/zzave/ynabsplitpayeeandmemo/e2e/SmokeTest.kt</files>
  <behavior>
    - Test: CLI invoked with --api-url pointing at WireMock succeeds
    - Test: WireMock receives request to expected endpoint
    - Test: CLI exits with expected code when mock server returns response
  </behavior>
  <action>
Create an E2E smoke test that validates the complete test infrastructure works end-to-end: WireMock mock server + ProcessBuilder CLI invocation.

**File:** `src/test/kotlin/com/github/zzave/ynabsplitpayeeandmemo/e2e/SmokeTest.kt`

**Implementation:**
- Extend `WireMockTestBase` for automatic WireMock lifecycle
- Create one test: "CLI can connect to WireMock mock server"
- Stub a minimal YNAB API endpoint (e.g., GET /v1/budgets returns empty budgets list)
- Use WireMock DSL: `stubFor(get(urlEqualTo("/v1/budgets")).willReturn(aResponse().withStatus(200).withBody("...")))`
- Invoke CLI via `runCli("--api-url", apiBaseUrl, "--token", "fake-token", "--dry-run")`
- Assert: exit code is non-error (0 or expected failure code)
- Verify: WireMock received request using `wireMockServer.verify(getRequestedFor(urlEqualTo("/v1/budgets")))`

**Minimal YNAB API stub response:**
```json
{
  "data": {
    "budgets": []
  }
}
```

**Why smoke test:** Validates that all pieces work together before writing actual business logic tests in Phase 3. If this passes, infrastructure is solid.

**Note:** Before running test, build test JAR with `./gradlew shadowJar -PtestBuild=true` in beforeSpec hook using Gradle Tooling API or document as prerequisite.
  </action>
  <verify>
    <automated>./gradlew shadowJar -PtestBuild=true && make test TEST="*SmokeTest*"</automated>
  </verify>
  <done>Smoke test passes: CLI successfully connects to WireMock mock server, WireMock receives request, full E2E flow validated</done>
</task>

</tasks>

<verification>
**Build test JAR:**
```bash
./gradlew shadowJar -PtestBuild=true
ls build/libs/*-all.jar
```
Should show test JAR exists

**Run smoke test:**
```bash
make test TEST="*SmokeTest*"
```
Should pass with CLI connecting to WireMock

**Verify CliRunner:**
```bash
make test TEST="*CliRunner*"
```
Should pass with ProcessBuilder invocation tests
</verification>

<success_criteria>
1. CliRunner.runCli() function executes test JAR via ProcessBuilder
2. CliResult captures exit code, stdout, and stderr from CLI subprocess
3. SmokeTest demonstrates full E2E infrastructure: WireMock + ProcessBuilder + CLI invocation
4. All tests pass including smoke test validating end-to-end flow
</success_criteria>

<output>
After completion, create `.planning/phases/02-test-infrastructure-setup/02-02-SUMMARY.md`
</output>
